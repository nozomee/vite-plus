[1]> vp migrate --no-interactive # migration should detect eslint and auto-migrate

⚠ ERR_PNPM_NO_MATCHING_VERSION  No matching version found for @oxlint/migrate@<semver> while fetching it from https://registry.<domain>/

The latest release of @oxlint/migrate is "1.61.0".

If you need the full list of all 85 published versions run "pnpm view @oxlint/migrate versions".

You can run `vp dlx @oxlint/migrate@<semver> --merge --type-aware --with-nursery --details` manually later
ESLint migration failed. Fix the issue and re-run `vp migrate`.


> cat package.json # check eslint removed and scripts rewritten
{
  "name": "migration-eslint",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "lint": "eslint .",
    "lint:fix": "eslint --fix ."
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "vite": "^7.0.0"
  },
  "packageManager": "pnpm@<semver>"
}
[1]> cat pnpm-workspace.yaml # check pnpm-workspace.yaml has overrides and catalog
cat: pnpm-workspace.yaml: No such file or directory

[1]> test ! -f eslint.config.mjs # check eslint config is removed
[1]> cat vite.config.ts # check oxlint config merged into vite.config.ts
cat: vite.config.ts: No such file or directory
