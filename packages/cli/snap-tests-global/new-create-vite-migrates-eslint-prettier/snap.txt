[1]> vp create vite@9.0.5 --no-interactive -- my-react-ts --template react-ts # create vite app with pinned version + react-ts template, should auto-migrate ESLint -> Oxlint and merge lint config into vite.config.ts

Using default package manager: pnpm

pnpm@latest installing...

pnpm@<semver> installed

Generating project…

Running: pnpm dlx create-vite@<semver> my-react-ts --template react-ts --no-immediate --no-rolldown
Packages: +<variable>
+<repeat>
Progress: resolved <variable>, reused <variable>, downloaded <variable>, added <variable>, done
│
◇  Scaffolding project in <cwd>/my-react-ts...
│
└  Done. Now run:

  cd my-react-ts
  pnpm install
  pnpm dev


Wrote agent instructions to AGENTS.md

Installing dependencies...

Dependencies installed

Migrating ESLint config to Oxlint...

ESLint migration failed

⚠ ERR_PNPM_NO_MATCHING_VERSION  No matching version found for @oxlint/migrate@<semver> while fetching it from https://registry.<domain>/

The latest release of @oxlint/migrate is "1.61.0".

If you need the full list of all 85 published versions run "pnpm view @oxlint/migrate versions".

You can run `vp dlx @oxlint/migrate@<semver> --merge --type-aware --with-nursery --details` manually later
ESLint migration failed.


[1]> test ! -f my-react-ts/eslint.config.js && echo 'eslint.config.js removed' # eslint config deleted
> test ! -f my-react-ts/.oxlintrc.json && echo '.oxlintrc.json merged into vite.config.ts' # migration output merged by rewrite step (matches vp migrate)
.oxlintrc.json merged into vite.config.ts

> cat my-react-ts/vite.config.ts # merged vite config should contain lint and fmt sections
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
})

> node -e "const p=require('./my-react-ts/package.json');console.log('lint:', p.scripts && p.scripts.lint);console.log('eslint dep:', !!(p.devDependencies && p.devDependencies.eslint));console.log('vite-plus dep:', !!(p.devDependencies && p.devDependencies['vite-plus']));" # scripts rewritten, eslint dep removed, vite-plus added
lint: eslint .
eslint dep: true
vite-plus dep: false
