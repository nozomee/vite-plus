[1]> vp migrate --no-interactive # migration should detect eslint in monorepo and migrate all packages

⚠ ERR_PNPM_NO_MATCHING_VERSION  No matching version found for @oxlint/migrate@<semver> while fetching it from https://registry.<domain>/

The latest release of @oxlint/migrate is "1.61.0".

If you need the full list of all 85 published versions run "$ pnpm view @oxlint/migrate versions".

You can run `vp dlx @oxlint/migrate@<semver> --merge --type-aware --with-nursery --details` manually later
ESLint migration failed. Fix the issue and re-run `vp migrate`.


> cat package.json # check root eslint removed and scripts rewritten
{
  "name": "migration-eslint-monorepo",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "lint": "eslint ."
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "vite": "^7.0.0"
  },
  "packageManager": "pnpm@<semver>"
}

> cat packages/app/package.json # check app eslint removed and scripts rewritten
{
  "name": "app",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "lint": "eslint .",
    "lint:fix": "eslint --fix ."
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "vite": "^7.0.0"
  }
}

> cat packages/utils/package.json # check utils eslint removed and scripts rewritten
{
  "name": "@test/utils",
  "scripts": {
    "lint": "eslint ."
  },
  "devDependencies": {
    "eslint": "^9.0.0"
  }
}

[1]> test ! -f eslint.config.mjs # check root eslint config is removed